int MiniMax(int a[][3], int n, int m) {
    int i,j,max,minimax;

    for(i=0;i<n;i++) {
        max = a[i][0];

        for(j=1;j<m;j++)
            if(a[i][j] > max)
                max = a[i][j];

        if(i==0 || max < minimax)
            minimax = max;
    }
    return minimax;
}
